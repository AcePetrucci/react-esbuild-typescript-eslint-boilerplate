import styled from 'styled-components';

// Themes
import { themes } from 'themes';

/**
 * Styled Components
 */

export const MainWrapper = styled.section`
  width: 100%;
  height: 100%;
  background-color: ${themes.dark.colors.body};
  color: ${themes.dark.colors.text}
`;
